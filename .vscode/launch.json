{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "engine_app (Debug)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/engine_app",
      "cwd": "${workspaceFolder}",
      "args": []
    }
  ]
}
